# =========================
# SERVER CONFIGURATION
# =========================
server:
  port: ${PORT:8080}

# =========================
# DATABASE CONFIGURATION (POSTGRESQL)
# =========================
spring:
  datasource:
    # URL para o banco de dados local 'barbearia'
    url: jdbc:postgresql://localhost:5432/barbearia
    # Usa a variável de ambiente DB_USER ou 'postgres' como padrão
    username: ${DB_USER:postgres}
    # Obriga o uso da variável de ambiente DB_PASSWORD para segurança
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    # 'update' sincroniza o banco com suas classes Java
    hibernate:
      ddl-auto: update
    show-sql: true 
    properties:
      hibernate:
        # O dialeto é detectado automaticamente, mas deixamos configurado para o Postgres
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

# =========================
# SECURITY (JWT)
# =========================
jwt:
  # Chave secreta protegida por variável de ambiente
  secret: ${JWT_SECRET}
  # Expiração de 24 horas (em milissegundos)
  expiration: 86400000

# =========================
# DEVELOPMENT CONTROL
# =========================
dev:
  owner:
    key: ${DEV_OWNER_KEY}